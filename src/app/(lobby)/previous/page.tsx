import { ContentLayout } from "@/components/admin-panel/content-layout";
import PreviousResources from "./_components/prevoius";

export default function Saved() {
  return (
    <ContentLayout title="Saved Resources">
      <PreviousResources />
    </ContentLayout>
  );
}
